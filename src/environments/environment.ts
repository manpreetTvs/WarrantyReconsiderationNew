// This file can be replaced during build by using the `fileReplacements` array.
// `ng build --prod` replaces `environment.ts` with `environment.prod.ts`.
// The list of file replacements can be found in `angular.json`.

export const environment = {
  production: false,
 apiUrlIndex: 'http://localhost:3000/api/',
  apiUrlApp: 'http://localhost:3001/api/',
  apiUrlSap: 'http://localhost:3002/',
 //host:'http://localhost:51539',
 //host:'http://localhost:51539',
 host: 'https://www.advantagetvs.com/ReconsiderationAPI',
  SAPHOst:'http://thshdevsrvr.hosur.tvsmotor.co.in:8000/sap/bc/zsd_war_rej_rec/war_rej_recon?sap-client=400'
};

/*
 * For easier debugging in development mode, you can import the following file
 * to ignore zone related error stack frames such as `zone.run`, `zoneDelegate.invokeTask`.
 *
 * This import should be commented out in production mode because it will have a negative impact
 * on performance if an error is thrown.
 */
// import 'zone.js/dist/zone-error';  // Included with Angular CLI.
